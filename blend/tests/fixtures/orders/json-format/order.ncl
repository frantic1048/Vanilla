{
  blend = {
    prefix = ["~/.config/json-format/"],
    files = [
      {
        name = "settings.json",
        from_config = {
          fontSize = 14,
          tabSize = 2,
          fontFamily = "JetBrains Mono",
        },
      },
    ],
  },
}
